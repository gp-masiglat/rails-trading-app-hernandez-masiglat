<div class="row text-center d-flex justify-content-center">


  <%= render "form", user: @user %>

  <br>

  <div>
  <% if current_user && current_user.role == 'Admin' %>
    <%= link_to "Back to users", users_path, :class => 'btn btn-primary btn-block m-2' %>
    <% end %>
  </div>
</div>
